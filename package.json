{
  "name": "apartment-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Point-of-sale for rooms for rent. Single-file app; this package.json exists ONLY to run the Playwright suite — the app itself has no build step (see docs/DECISIONS.md D-001).",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "postinstall": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}

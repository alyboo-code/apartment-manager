🌅 *apartment-manager — Morning Digest*
Fri 24 Jul · 5 proposals waiting · 🎯 Objective: *Close the paths where a transient failure becomes permanent data loss*

✅ *RECOMMEND APPROVE (5)*
*2* · "Go to Billing" lands on the wrong month and highlights the wrong tab
   → the dashboard tells the landlord which month is missing a bill, then
*3* · Quick Entry's Total Due disagrees with the bill it actually saves
   → the fast path shows the landlord one number and writes a different one.
*4* · Bill Sheet omits moved-out tenants who still owe for that month
   → the caretaker's collection sheet is missing balances that are still owed.
*5* · Background refresh silently discards meter readings typed into the Billing grid
   → leaving the app for ten seconds mid-entry wipes unsaved readings with no
*6* · Every period selector defaults to the current month, but the work is always last month
   → seven selectors default to a month the landlord is usually not working on,

—
*Reply:* `Accept` (take all my recs) · `Approve all` · `Approve 14-19` · `Park 7` · `Reject 12`
Approved → built next run. Silence → nothing happens.
